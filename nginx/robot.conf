# Block unwanted bots and crawlers
# Apply this inside server{ location / { ... } }

if ($http_user_agent ~* "(bot|crawler|spider|curl|wget|AhrefsBot|SemrushBot|MJ12bot|Baiduspider|DotBot)") {
    return 404;
}
